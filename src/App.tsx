import React, { FC, ReactElement } from "react";
import Article from "./components/article";

const App: React.FC = () => {
  return (
    <div>
      <p>this is a page</p>
    </div>
  );
};

export default App;
